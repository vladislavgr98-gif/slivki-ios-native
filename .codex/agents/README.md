# Project Codex Agents

Codex TOML agents from [`VoltAgent/awesome-codex-subagents`](https://github.com/VoltAgent/awesome-codex-subagents).

**Active Cursor crew (wired):** `.cursor/agents/` + rule `.cursor/rules/slivki-crew.mdc`
+ root `AGENTS.md`. Those 12 are what day-to-day work should use.

## Active Cursor crew (12)

| Agent | Role |
| --- | --- |
| `swift-expert` | SwiftUI / iOS |
| `mobile-app-developer` | Multi-screen flows |
| `ui-fixer` | Small UI patches |
| `ui-designer` | Layout decisions |
| `php-pro` | Prod PHP / API |
| `api-designer` | mobile-v1 contracts |
| `debugger` | Failures |
| `browser-debugger` | Site evidence |
| `reviewer` | Diff review |
| `qa-expert` | Acceptance QA |
| `deployment-engineer` | Deploy / release |
| `security-auditor` | Secrets / auth |

## Broader Codex set (on demand)

Still available here as `.toml` (not all converted to Cursor):
`accessibility-tester`, `backend-developer`, `code-reviewer`,
`documentation-engineer`, `technical-writer`, `git-workflow-manager`,
`performance-engineer`, `ui-ux-tester`, `agent-installer`,
`devops-incident-responder`, `security-engineer`, `error-detective`,
`test-automator`, `incident-responder`.

## SSH

```bash
ssh slivki-prod
ssh slivki-prod-admin
```
